// ============================================================
//  document.cpp — Document struct implementation
//
//  Currently the Document struct is all data (no methods),
//  but we provide this compilation unit so the CMake library
//  has something to compile. If methods are added later (e.g.
//  save(), metadata()), they go here.
// ============================================================

#include "types.h"

namespace scan_master {

// All Document methods are inline in types.h for now.
// This file exists as a placeholder for future methods
// and to satisfy CMake's requirement that library targets
// have at least one source file.

} // namespace scan_master
